DATABASE_URL="postgresql://listitup:listitup@localhost:5432/listitup_test?schema=public"
REDIS_URL="redis://localhost:6379"
MAILPIT_API_URL="http://localhost:8025"
MAILPIT_SMTP_HOST="localhost"
MAILPIT_SMTP_PORT="1025"

# Required by existing authentication behavior tests.
BETTER_AUTH_SECRET="test-secret-with-at-least-32-characters"
BETTER_AUTH_URL="http://localhost:3000"

# Test-only Mailpit transport configuration. Never use production SMTP values here.
MAIL_PROVIDER="custom"
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_SECURE="false"
SMTP_USER="test"
SMTP_PASSWORD="test"
MAIL_FROM_NAME="ListItUp"
MAIL_FROM_EMAIL="no-reply@listitup.test"
